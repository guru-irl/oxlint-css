import type { Colord } from "./colord";
import type postcssValueParser from "postcss-value-parser";
import { AbsColor } from "./color-class";
import type { AlphaArgument, AlphaArgumentValid, FunctionArgument, NumberWithUnit, ValuesArgument, ValuesArgumentComplete } from "./parser";
export declare class ColorFromGray extends AbsColor {
    private readonly gray;
    constructor(gray: GrayData | IncompleteGrayData);
    readonly type = "gray";
    isComplete(): boolean;
    getAlpha(): number | null;
    removeAlpha(): ColorFromGray;
    toColorString(): string;
    protected newColord(): Colord | null;
}
export type LightnessValue = NumberWithUnit<"" | "%">;
export type GrayData = {
    complete: true;
    rawName: string;
    lightness: ValuesArgumentComplete<LightnessValue>;
    alpha: AlphaArgumentValid | null;
    extraArgs?: undefined;
};
export type IncompleteGrayData = {
    complete: false;
    rawName: string;
    lightness: ValuesArgument<LightnessValue>;
    alpha: AlphaArgument | null;
    extraArgs: FunctionArgument[];
};
export declare function parseGray(input: string | postcssValueParser.Node): GrayData | IncompleteGrayData | null;
