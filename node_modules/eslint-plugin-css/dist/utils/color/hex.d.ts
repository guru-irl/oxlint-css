import type { Colord } from "./colord";
import type postcssValueParser from "postcss-value-parser";
import { AbsColor } from "./color-class";
export declare class ColorFromHex extends AbsColor {
    private readonly hex;
    constructor(hex: HexData);
    readonly type = "hex";
    getRgb(): {
        r: number;
        g: number;
        b: number;
    };
    isComplete(): boolean;
    getAlpha(): number | null;
    removeAlpha(): ColorFromHex;
    toColorString(): string;
    protected toHexImpl(): string;
    private parseColor;
    protected newColord(): Colord;
}
export declare function isHex(str: string): boolean;
export type HexData = {
    readonly kind: "RGB" | "RRGGBB";
    readonly r: string;
    readonly g: string;
    readonly b: string;
    readonly alpha?: string;
};
export declare function parseHex(input: string | postcssValueParser.Node): HexData | null;
