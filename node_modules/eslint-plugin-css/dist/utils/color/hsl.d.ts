import type { Colord } from "colord";
import type postcssValueParser from "postcss-value-parser";
import { AbsColor } from "./color-class";
import type { AlphaArgument, AlphaArgumentValid, FunctionArgument, NumberWithUnit, ValuesArgument, ValuesArgumentComplete } from "./parser";
export declare class ColorFromHsl extends AbsColor {
    private readonly hsl;
    constructor(hsl: HslData | IncompleteHslData);
    readonly type = "hsl";
    isComplete(): boolean;
    getAlpha(): number | null;
    removeAlpha(): ColorFromHsl;
    toColorString(): string;
    protected newColord(): Colord;
}
export type HslValue = {
    hue: NumberWithUnit<"" | "deg" | "rad" | "grad" | "turn">;
    saturation: NumberWithUnit<"%">;
    lightness: NumberWithUnit<"%">;
};
export type HslData = {
    complete: true;
    rawName: string;
    hsl: ValuesArgumentComplete<HslValue>;
    alpha: AlphaArgumentValid | null;
    extraArgs?: undefined;
};
export type IncompleteHslData = {
    complete: false;
    rawName: string;
    hsl: ValuesArgument<HslValue>;
    alpha: AlphaArgument | null;
    extraArgs: FunctionArgument[];
};
export declare function parseHsl(input: string | postcssValueParser.Node): HslData | IncompleteHslData | null;
