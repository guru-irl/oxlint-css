import type { ImportDeclaration, SimpleCallExpression } from "estree";
import type { Rule } from "eslint";
export type CallReference = {
    node: SimpleCallExpression;
    paths: readonly string[][];
};
export declare function extractCallReferences(node: ImportDeclaration, functionPaths: string[][], context: Rule.RuleContext): Iterable<CallReference>;
